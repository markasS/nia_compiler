{
  ЛОГИЧЕСКОЕ и1 = 5 > 2;
  ЛОГИЧЕСКОЕ и = !и1;
  ЕСЛИ (и == ВЕРНО)  {
     ПИСАТЬ ("if", и);
  }
  ИНАЧЕ {
  ПИСАТЬ ("else", и);
  }

  СТРОКА а = "eloquence";
  СТРОКА ц = ДОЛЯ(а, 2, 3);
  ц = "YDAD" + а + ("1" + ДОЛЯ("me", 0, 1)) + '7' + В_СТРОКУ(88);
  ПИСАТЬ (ц);
}

